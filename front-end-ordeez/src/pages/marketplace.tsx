'use client'; // This is a client component

import '@/styles/globals.css';
import { ReactNode, memo } from 'react';

const Marketplace = ({ children }: { children: ReactNode }) => {
  return children;
};

export default Marketplace;
