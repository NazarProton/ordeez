import { Dispatch, SetStateAction } from 'react';

const FooterFilters = ({
  setIsOnlyMigrated,
  isOnlyMigrated,
}: {
  setIsOnlyMigrated: Dispatch<SetStateAction<boolean>>;
  isOnlyMigrated: boolean;
}) => {
  return <div></div>;
};

export default FooterFilters;
